import { OllamaProvider } from "./OllamaProvider.js";

// Currently hardcoded to OllamaProvider. Can be dynamically routed later based on model selection.
export const llmProvider = new OllamaProvider();
